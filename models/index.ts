import { User, UserLog } from "./user";
import { Project, ProjectLog } from "./project";
import { Milestone } from "./milestone";

export const lists = {
  User,
  UserLog,
  Project,
  ProjectLog,
  Milestone,
};
